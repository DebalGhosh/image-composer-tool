/**
 * compose-basic — cascading-dropdown composer.
 *
 * LiveYamlPreview is private: it reads the same selection the page owns, so a
 * second mount point for it would need a second source of that state.
 */
export { BasicPage } from './BasicPage'
