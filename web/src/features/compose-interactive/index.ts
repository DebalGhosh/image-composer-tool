/**
 * compose-interactive — form-driven CoreV1 composer.
 *
 * InteractiveYamlPreview stays private for the same reason as compose-basic's:
 * it is bound to the draft this page owns.
 */
export { InteractivePage } from './InteractivePage'
